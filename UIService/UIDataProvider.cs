using ECGaming.UISystem;
using ECGaming.UISystem.Interfaces;

namespace InternalAssets.Scripts.Infrastructure.Services.UISystem
{
    public class UIDataProvider
    {
        private readonly UIDirector _uiDirector;

        public UIDataProvider(UIDirector uiDirector)
        {
            _uiDirector = uiDirector;
        }

        
    }
}